# Examples

This folder holds small committed examples of generated research artifacts. Live reports are written to `data/` and ignored by Git so local results and account-specific state stay private.
